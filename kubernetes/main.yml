apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels:
    app: main
spec:
  replicas: 1
  selector:
    matchLabels:
      app: main
  template:
    metadata:
      labels:
        app: main

    spec:
      containers:
      - name: main
        image: ${DOCKER_USERNAME}/app:${TIMESTAMP}
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer
  selector:
    app: main
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000